'use strict';

/**
 * matching-game-word router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::matching-game-word.matching-game-word');
