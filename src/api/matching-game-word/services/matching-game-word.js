'use strict';

/**
 * matching-game-word service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::matching-game-word.matching-game-word');
