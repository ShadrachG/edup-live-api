'use strict';

/**
 * quiz-game-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::quiz-game-question.quiz-game-question');
