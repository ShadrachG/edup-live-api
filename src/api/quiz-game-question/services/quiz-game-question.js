'use strict';

/**
 * quiz-game-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::quiz-game-question.quiz-game-question');
